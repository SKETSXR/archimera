## VS Code Recommendations
- Open the `backend` folder as the workspace root in VS Code for backend development.
- Install recommended extensions: ESLint, Prettier, Node Debugger.
- Use the integrated terminal to run `npm install` and `npm run dev`.
